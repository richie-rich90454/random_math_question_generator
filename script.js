let quotesArray=[];
let authorsArray=[];
fetch('/quotes_of_the_day.txt').then(response=>response.text()).then(data=>{const lines=data.split('\n').map(line=>line.trim()).filter(line=>line.length > 0);lines.forEach((line)=>{const parts=line.split('-');if (parts.length < 2) {console.log(`Invalid line format: ${line}`);return;}const quote=parts.slice(0, -1).join('-').trim();const author=parts.slice(-1)[0].trim();quotesArray.push(quote);authorsArray.push(author);});console.log("Quotes:", quotesArray);console.log("Authors:", authorsArray);}).catch(error=>console.error('Error loading quotes:', error));